Review Aphelion's recent sessions, memory layer, prompts, runtime configuration, and codebase behavior; report the state of things with evidence; and propose focused, verifiable fixes without mutating the local Aphelion clone.

When implementation is approved, work only in an isolated `/tmp` clone and propose the result via GitHub PR using an approved GitHub App credential PEM.
