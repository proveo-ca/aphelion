# Capability Request

- current_blocker:
- evidence:
- minimal_capability:
- target_resource:
- allowed_actions:
- success_metric:
- trial_boundary:
- rollback:

For implementation or PR work, request a bounded grant for a `/tmp` clone plus a separately approved GitHub App credential PEM. Do not request permission to mutate the local Aphelion clone.
