//go:build linux

// Package workspace owns workspace prompt summaries.
//
// It reports local workspace context for prompt assembly without deciding tool
// permissions or runtime behavior.
package workspace
